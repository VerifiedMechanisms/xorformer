# Prompt of the counterexample search

The single user message of the retained turn, extracted verbatim from `rollouts/separation-rollout.jsonl` (the redacted rollout). Harness-injected context blocks are omitted. Timestamp is UTC.

1 message.

## 1. 2026-07-13 16:12:30

```text
/goal Find an explicit constructive example when deg_\pm(f) not equal to h*(f)
```
